{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "TreatyLibrary",
  "title": "Treaty library options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the new library.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the library?"
    },
    "prefix": {
      "type": "string",
      "description": "The component selector prefix for the library.",
      "default": "lib"
    }
  },
  "required": ["name"]
}
