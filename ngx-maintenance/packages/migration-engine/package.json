{
  "name": "@ngx-maintenance/migration-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic Angular migration chain (vX..latest via official ng update schematics + VE->Ivy + curated codemods).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "lint": "oxlint -c ../../.oxlintrc.json src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@ngx-maintenance/registry": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
