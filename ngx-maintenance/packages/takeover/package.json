{
  "name": "@ngx-maintenance/takeover",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Takeover policy engine: detect unmaintained libs (2-week non-merge) and stage maintained @ngx-maintenance forks.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.lib.json",
    "lint": "oxlint -c ../../.oxlintrc.json src",
    "test": "vitest run"
  },
  "dependencies": {
    "@ngx-maintenance/registry": "workspace:*"
  },
  "devDependencies": {
    "vitest": "*"
  }
}
